"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function MemberRegisterPage() { const router = useRouter(); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setSaving(true); setError(""); const f = new FormData(e.currentTarget); const r = await fetch("/api/members/register", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: f.get("name"), email: f.get("email"), phone: f.get("phone"), password: f.get("password") }) }); const data = await r.json(); if (!r.ok) { setError(data.error); setSaving(false); return; } router.push("/member/login?registered=1"); } const input = "h-12 rounded-lg border border-border bg-background px-4 outline-none focus:border-primary"; return <div className="mx-auto max-w-md px-4 py-10"><p className="text-sm font-bold uppercase tracking-[.18em] text-primary">Arena member</p><h1 className="mt-2 text-3xl font-extrabold">Daftar member</h1><p className="mt-2 text-muted">Simpan data untuk booking berikutnya dan melihat riwayat transaksi.</p><form onSubmit={submit} className="mt-6 grid gap-4 rounded-xl border border-border bg-surface p-5"><input required name="name" placeholder="Nama lengkap" className={input} /><input required name="email" type="email" placeholder="Email" className={input} /><input required name="phone" type="tel" placeholder="WhatsApp, contoh 0812..." className={input} /><input required name="password" type="password" minLength={8} placeholder="Password minimal 8 karakter" className={input} />{error ? <p className="text-sm text-red-300">{error}</p> : null}<button disabled={saving} className="rounded-lg bg-primary py-3 font-extrabold text-primary-foreground">{saving ? "Mendaftarkan..." : "Daftar sekarang"}</button></form><p className="mt-4 text-center text-sm text-muted">Sudah punya akun? <Link href="/member/login" className="font-bold text-primary">Masuk</Link></p></div>; }

"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
import { signIn } from "next-auth/react";
import { useRouter } from "next/navigation";
export default function MemberLoginPage() { const router = useRouter(); const [error, setError] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); const r = await signIn("credentials", { email: f.get("email"), password: f.get("password"), redirect: false }); if (r?.error) { setError("Email atau password salah."); return; } router.push("/member"); } const input = "h-12 rounded-lg border border-border bg-background px-4 outline-none focus:border-primary"; return <div className="mx-auto max-w-md px-4 py-10"><p className="text-sm font-bold uppercase tracking-[.18em] text-primary">Arena member</p><h1 className="mt-2 text-3xl font-extrabold">Masuk member</h1><form onSubmit={submit} className="mt-6 grid gap-4 rounded-xl border border-border bg-surface p-5"><input required name="email" type="email" placeholder="Email" className={input} /><input required name="password" type="password" placeholder="Password" className={input} />{error ? <p className="text-sm text-red-300">{error}</p> : null}<button className="rounded-lg bg-primary py-3 font-extrabold text-primary-foreground">Masuk</button></form><p className="mt-4 text-center text-sm text-muted">Belum member? <Link href="/member/daftar" className="font-bold text-primary">Daftar</Link></p></div>; }

"use client";
import { useState } from "react";
const days = ["Minggu", "Senin", "Selasa", "Rabu", "Kamis", "Jumat", "Sabtu"];
export function OperatingHoursForm({ initial }: { initial: { dayOfWeek: number; opensAt: string; closesAt: string; isOpen: boolean }[] }) { const [hours, setHours] = useState(initial); async function save() { await fetch("/api/admin/operating-hours", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify({ hours }) }); location.reload(); } return <section className="mt-6 rounded-xl border border-border bg-surface p-5"><h2 className="text-xl font-extrabold">Jam operasional</h2><div className="mt-4 grid gap-3">{hours.map((hour, i) => <div key={hour.dayOfWeek} className="grid grid-cols-[1fr_auto_auto_auto] items-center gap-2 text-sm"><span>{days[hour.dayOfWeek]}</span><input type="time" value={hour.opensAt} disabled={!hour.isOpen} onChange={(e) => setHours(hours.map((x, n) => n === i ? { ...x, opensAt: e.target.value } : x))} className="rounded border border-border bg-background p-2"/><input type="time" value={hour.closesAt} disabled={!hour.isOpen} onChange={(e) => setHours(hours.map((x, n) => n === i ? { ...x, closesAt: e.target.value } : x))} className="rounded border border-border bg-background p-2"/><input type="checkbox" checked={hour.isOpen} onChange={(e) => setHours(hours.map((x, n) => n === i ? { ...x, isOpen: e.target.checked } : x))}/></div>)}</div><button onClick={save} className="mt-5 rounded-lg bg-primary px-5 py-3 font-extrabold text-primary-foreground">Simpan jam operasional</button></section>; }

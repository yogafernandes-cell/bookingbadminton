"use client";
import { Menu, X } from "lucide-react";
import { useState } from "react";
import { AdminNavLinks } from "@/components/admin/admin-nav-links";
import { AdminLogoutButton } from "@/components/admin/admin-logout-button";
export function AdminMobileNav() { const [open, setOpen] = useState(false); return <><header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-border bg-surface px-4 md:hidden"><p className="font-extrabold text-primary">Booking Lapangan</p><button onClick={() => setOpen(true)} aria-label="Buka menu" className="rounded-lg border border-border p-2"><Menu className="size-5" /></button></header>{open ? <div className="fixed inset-0 z-50 bg-background/70" onClick={() => setOpen(false)}><aside className="flex h-full w-72 flex-col bg-surface p-5" onClick={(event) => event.stopPropagation()}><div className="flex items-center justify-between"><p className="text-xl font-extrabold text-primary">Admin</p><button onClick={() => setOpen(false)}><X /></button></div><nav className="mt-7 grid gap-2 overflow-y-auto"><AdminNavLinks onNavigate={() => setOpen(false)} /></nav><div className="mt-auto border-t border-border pt-4"><AdminLogoutButton /></div></aside></div> : null}</>; }
